import { createBranch, getRefsHead, createIssue } from "./utils";

// getRefsHead();
// createBranch();
// createPullRequest("Title PR1", "Test first PR !!!");
createIssue("Title Issue1", "Test creation first issues !!!");
